---
framework: jest
preRun:
     command:
        - npm ci
preMerge:
  pattern:
    - "./index.test.js"
postMerge: 
  pattern: 
    - "./index.test.js"
postRun:
     command:
        - node --version
version: 1.0
